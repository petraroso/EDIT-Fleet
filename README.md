# EDIT-Fleet

Run backend (port 5000): npm run start

Run frontend (port 3000): npm start

Replace dotenv.config({ path: ".env.local" }); with dotenv.config();

Replace MONGO_URI url with a working one.